---
interface Props { cfg: any; t: any; lang: string }
const { cfg, t, lang } = Astro.props
const a = cfg.about
---

<div class="about-split">
  <div class="about-split__text">
    <span class="eyebrow">{a.eyebrow}</span>
    <h2>{a.mission_headline}</h2>
    <p>{a.body}</p>
    <p>{a.mission_body_1}</p>
    <a class="btn btn-primary" href={`/${lang}/about`} style="margin-top:24px;width:fit-content;">{t.about.meet_team_btn}</a>
  </div>
  <div class="about-split__img">
    <span style="position:relative;z-index:1;">{a.mission_emoji}</span>
  </div>
</div>
